import { ImageHelperPipe } from './image-helper.pipe';

describe('ImageHelperPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageHelperPipe();
    expect(pipe).toBeTruthy();
  });
});
